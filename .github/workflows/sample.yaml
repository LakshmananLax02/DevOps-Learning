name: Sample pipeline

on: 
   push:
       branches: ["main"]

jobs:
    build:
        runs-on: [self-hosted, wsl-ubuntu]       

        steps:
            - name: Sample step
              run: echo "Sample pipeline"

    deploy:
        runs-on: [self-hosted, wsl-ubuntu]

        steps:
             - name: Deploy step
               run: echo "Deploye pipeline"
               
         
